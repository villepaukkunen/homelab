global:
  namespace: openbao

server:
  dataStorage:
    enabled: true
    size: 500Mi
    mountPath: "/openbao/data"
    storageClass: longhorn
    accessMode: ReadWriteOnce
  ha:
    enabled: true
    replicas: 3
    raft:
      enabled: true

ui:
  enabled: true
  activeOpenbaoPodOnly: true