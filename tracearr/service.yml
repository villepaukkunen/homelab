---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: tracearr
  name: tracearr
  namespace: tracearr
spec:
  selector:
    app: tracearr
  ports:
    - name: web
      port: 3000
      protocol: TCP
      targetPort: web
