apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: jellyfin
  name: jellyfin
  namespace: jellyfin
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: jellyfin
  template:
    metadata:
      labels:
        app: jellyfin
    spec:
      nodeSelector:
        streaming: "true"
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: hotio
          image: ghcr.io/hotio/base:alpinevpn-663c831-615-linux-amd64
          imagePullPolicy: IfNotPresent
          restartPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              add:
                - NET_ADMIN
          ports:
            - containerPort: 8096
              name: web
              protocol: TCP
          env:
            - name: TZ
              value: Europe/Helsinki
            - name: VPN_ENABLED
              value: "true"
            - name: VPN_LAN_LEAK_ENABLED
              value: "true"
            - name: VPN_LAN_NETWORK
              value: '10.43.0.0/16'
            - name: VPN_NAMESERVERS
              value: '10.43.0.10'
          envFrom:
            - secretRef:
                name: vpn-credentials
      containers:
        - name: jellyfin
          image: ghcr.io/jellyfin/jellyfin:10.11.11
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            capabilities:
              drop:
                - "ALL"
          env:
            - name: TZ
              value: Europe/Helsinki
            - name: JELLYFIN_PublishedServerUrl
              value: https://jellyfin.mandariini.uk
          resources:
              requests:
                  gpu.intel.com/i915: "1"
              limits:
                  gpu.intel.com/i915: "1"
          volumeMounts:
            - mountPath: /run
              name: jellyfin
              subPath: run
            - mountPath: /cache/transcodes
              name: emptydir
              subPath: transcodes
            - mountPath: /config
              name: jellyfin
              subPath: config
            - mountPath: /cache
              name: jellyfin
              subPath: cache
            - mountPath: /data/movies
              name: media
              subPath: movies
            - mountPath: /data/tvseries
              name: media
              subPath: tvseries
            - mountPath: /data/livetv
              name: media
              subPath: livetv
            - mountPath: /config/config/logging.json
              name: config
              subPath: logging.json
      volumes:
        - name: jellyfin
          persistentVolumeClaim:
            claimName: jellyfin
        - name: emptydir
          emptyDir: {}
        - name: media
          persistentVolumeClaim:
            claimName: media
        - name: config
          configMap:
            name: config