apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-all-gotify-server
  namespace: gotify
spec:
  podSelector:
    matchLabels:
      app: gotify
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 80
          protocol: TCP
